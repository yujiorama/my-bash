#!/bin/bash
PATH=$(echo "$PATH" | tr ':' '\n' | grep -v -e '^$' | grep -v -E '[rR]uby|rbenv' | tr '\n' ':')

export RBENV_ROOT
RBENV_ROOT="${HOME}/.rbenv"

export PATH
PATH="${RBENV_ROOT}/bin:${PATH}"

export PATH
if [[ -d "${HOME}/scoop/apps/ruby/current" ]]; then
    PATH="${PATH}:${HOME}/scoop/apps/ruby/current/bin"
fi
